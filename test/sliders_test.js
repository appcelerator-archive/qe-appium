'use strict';

const Setup = require('../helpers/setup.js');

// Controls > Slider > Basic
describe('KS sliders on android', function () {
	before('', function () {

	});

	after('', function () {

	});

	it('do something there', function () {

	});
});

describe('KS sliders on ios', function () {
	before('', function () {

	});

	after('', function () {

	});

	it.skip('do something here', function () {

	});
});