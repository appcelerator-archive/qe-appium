'use strict';

const Setup = require('../helpers/setup.js');

// Controls > Label > Basic
describe('KS Label on Android', function () {
	before('', function () {

	});

	after('', function () {

	});

	it('do something there', function () {

	});
});

describe('KS Label on iOS', function () {
	before('', function () {

	});

	after('', function () {

	});

	it.skip('do something here', function () {

	});
});